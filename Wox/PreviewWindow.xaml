<Window x:Class="Wox.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Wox.ViewModel"
        xmlns:local="clr-namespace:Wox"
        mc:Ignorable="d"
        Topmost="True"
        Title="PreviewWindow" 
        WindowStartupLocation="CenterOwner"
        Style="{DynamicResource WindowStyle}"
        Icon="app.png"
        Width="800"
        Height="600"
        d:DataContext="{d:DesignInstance vm:PreviewViewModel}"
        Visibility="{Binding MainWindowVisibility, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding EscCommand}"></KeyBinding>
    </Window.InputBindings>
    <Border Style="{StaticResource WindowBorderStyle}">
        <Grid>
            <ContentControl Content="{Binding PreviewContent}" Margin="10"/>
        </Grid>
    </Border>
</Window>
